post:
  summary: This is a post route for sales record
  description: Sales record
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/sales.yaml#/salesInput'
  responses:
    '201':
      description: New sale record
      content:
        application/json:
          schema:
            $ref: '../components/schemas/sales.yaml#/salesOutput'
    '400':
      description: Unable to save record, please try again!
      content:
        application/json:
          schema:
            $ref: '../components/schemas/error.yaml#/errorResponse'
            
get: 
  summary: This is the get route for sale record
  description: get all sale record
  responses:
    '200':
      description: Sales fetched successfully
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../components/schemas/sales.yaml#/salesOutput'